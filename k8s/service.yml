apiVersion: v1
kind: Service
metadata:
  name: maven-app-service
  namespace: dev
spec:
  selector:
    app: maven-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: NodePort
